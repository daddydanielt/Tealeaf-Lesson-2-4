<!-- =================================================== -->
<!-- <h4>Method#3: Rails model-backed formm helpers</h4> -->
<!-- You can do a lot of ways. -->
<!-- form_for Post.new -->
<!-- form_for Post.first-->
<!-- @post= Post.new 
     form_for @post 
-->
<%= render 'shared/errors', errors: @post.errors %>

<%= form_for @post do |f| %>
  <div class="well">
    <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title, class: 'form-control' %>
    </div>
    <div class="form-group">
      <%= f.label :description %>  
      <%= f.text_area :description, rows: 5, class: 'form-control' %>
    </div>
    <div class="form-group">
      <%= f.label :url %>  
      <%= f.url_field :url, class: 'form-control' %>
    </div>

    <div class="form-group">
    <%= f.collection_check_boxes :category_ids, Category.all, :id, :name do |cb| %>                     
      <% ("<div class='checkbox'>" + cb.label { cb.check_box(class: "checkbox") + cb.text } + "</div>").html_safe %>
    <% end %>    
    </div>

    <%#Auto generate Submit Button%>
    <%#= f.button class:"btn btn-primary" %>
    <div class="form-group">
      <% if @post.new_record? %>     
        <%= f.submit("Create", class: 'btn btn-default') %>
      <% else %>
        <%= f.submit("Update", class: 'btn btn-default') %>
      <% end %>
    </div>

 

   
<% end %>





